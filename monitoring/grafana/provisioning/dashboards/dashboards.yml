# Purpose: load dashboard JSON files automatically when Grafana starts.
apiVersion: 1

providers:
  - name: omnichannel
    orgId: 1
    folder: Omnichannel
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
